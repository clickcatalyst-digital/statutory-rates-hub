import './globals.css';

export const metadata = { title: 'Statutory Rates Hub' };

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
